// Write code to sum an array of numbers.

function basicSum(arr) {
    // Using reduce():
    // return arr.reduce((a, n) => a + n)

    // Basic:
    // let sum = 0;
    // for (let n of arr) { sum += n }
    // return sum;
}

module.exports = { basicSum }